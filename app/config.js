/* FitTrack — deployment config (ES module).
   The Supabase project every install talks to. The publishable/anon key is
   designed to be public: data is protected by each user's sign-in + row-level
   security, not by hiding this key. Fill in once per deployment. */
export const CONFIG = {
  supabaseUrl: 'https://ahsgvjugkmzuascvaclm.supabase.co',
  supabaseKey: 'sb_publishable_uOqDR-bKo5c0a9CHvn_TiQ_mG0XnI38',
};
